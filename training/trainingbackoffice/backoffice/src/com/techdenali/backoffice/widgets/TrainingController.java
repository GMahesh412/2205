package com.techdenali.backoffice.widgets;

public class TrainingController
{

	/*
	 * @WireVariable("widgetInstanceManager") private WidgetInstanceManager wim;
	 *
	 * @WireVariable private SiteFacade siteFacade;
	 *
	 * @SocketEvent(socketId = "nodeSelected") public void nodeSelected(final Object data) { if (data instanceof
	 * NavigationNode) { final String nodeId = wim.getWidgetSettings().getString("relatedExplorerTreeNode"); if
	 * (((NavigationNode) data).getId().equals(nodeId)) { final String currentWidgetId =
	 * wim.getWidgetslot().getWidgetInstance().getId(); wim.sendOutput("selectPerspectiveById", currentWidgetId); } } }
	 *
	 * @Command public void displaySiteInfo(@BindingParam("siteUid") final String siteUid) {
	 * siteFacade.getAllSiteData().stream().filter(o -> o.getUid().equals(siteUid)).findFirst().ifPresent(o -> {
	 * Messagebox.show(String.join(", ", o.getContentCatalogs())); });
	 *
	 * }
	 *
	 * public List<SiteData> getAllSiteData() { return siteFacade.getAllSiteData(); }
	 */

}